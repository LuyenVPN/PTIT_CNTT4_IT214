# ============================================================
# File: delivery-service.yml
# Service: delivery-service (spring.application.name = "delivery-service")
#
# Quy ước đặt tên:
#   Config Server resolve file theo {spring.application.name}.yml
#   → delivery-service.yml  (file này đặt đúng tên)
# ============================================================

spring:
  datasource:
    url: jdbc:mysql://foodx-cluster.local:3306/delivery_db
    username: delivery_service_user
    # Mật khẩu được mã hóa bằng symmetric key của Config Server
    password: "{cipher}DTE0pRsY5uMzOwNvBcAqJiGeLfHkQdPn..."

  data:
    redis:
      host: foodx-redis.local
      port: 6379
      # Redis auth token cũng cần mã hóa
      password: "{cipher}EUF1qStZ6vNaPoOwCdBrKjHfMgIlRePo..."

server:
  port: 8087

tracking:
  update-interval-ms: 5000
