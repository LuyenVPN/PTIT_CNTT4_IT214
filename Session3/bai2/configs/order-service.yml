# ============================================================
# File: order-service.yml
# Service: order-service (spring.application.name = "order-service")
#
# Quy ước đặt tên:
#   Spring Cloud Config Server tra cứu file theo thứ tự ưu tiên:
#     1. {appName}-{profile}.yml   (vd: order-service-prod.yml)
#     2. {appName}.yml             (vd: order-service.yml)  ← file này
#     3. application-{profile}.yml (cấu hình chung theo môi trường)
#     4. application.yml           (cấu hình chung mặc định)
# ============================================================

spring:
  datasource:
    url: jdbc:mysql://foodx-cluster.local:3306/orders_db
    username: order_service_user
    # Mật khẩu được mã hóa bằng symmetric key của Config Server
    password: "{cipher}BRC8nPqW3sKxMvLtZaYuHgEcJdFiObNm..."

  rabbitmq:
    host: foodx-mq.local
    port: 5672
    username: order_mq_user
    # Mật khẩu MQ cũng cần mã hóa
    password: "{cipher}CSD9oQrX4tLyNwMuAbZvIhFdKeGjPcOl..."

server:
  port: 8086
